{
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["{projectRoot}/input.txt"],
      "outputs": ["{projectRoot}/output.txt"],
      "options": {
        "command": "echo foo > projects/foo/output.txt"
      }
    }
  }
}
