{
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["{workspaceRoot}/**/*"],
      "outputs": ["{projectRoot}/output.txt"],
      "options": {
        "command": "echo foo > projects/foo/output.txt"
      }
    }
  }
}
